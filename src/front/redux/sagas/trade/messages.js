import { defineMessages } from 'react-intl';

export default defineMessages({
  connectionError: 'Connection Error',
  connectionErrorMessage: 'Fetching trade histories from {provider} has been failed. The provider\'s server might down.',
  fetchTrades: 'Fetching trade histories from {provider}',
});

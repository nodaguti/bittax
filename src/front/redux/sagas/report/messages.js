import { defineMessages } from 'react-intl';

export default defineMessages({
  fetchingPrices: 'Fetching missing price data',
  makingReportFailed: 'Internal Error',
  makingReportFailedMessage: 'Making the trade report has been failed.',
});

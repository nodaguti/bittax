import { defineMessages } from 'react-intl';

export default defineMessages({
  authenticationError: 'Authentication Error',
  authenticationErrorMessage:
    'Unable to receive authentication data from {provider}. Please try again.',
  saving:
    'Saving authentication data and returning back to the original window...',
});

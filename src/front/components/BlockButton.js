import styled from 'styled-components';
import {
  Button,
  ButtonOutline,
} from 'rebass';

export const BlockButton = styled(Button)`
  display: block;
  width: 100%;
`;

export const BlockButtonOutline = styled(ButtonOutline)`
  display: block;
  width: 100%;
`;
